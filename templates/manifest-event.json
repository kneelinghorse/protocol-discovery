{
  "type": "{{type}}",
  "name": "{{name}}",
  "version": "{{version}}",
  "description": "{{description}}",
  "protocol": {
    "transport": "{{transport}}",
    "events": [
      {
        "name": "{{event_name}}",
        "schema": {},
        "description": "{{event_description}}"
      }
    ]
  },
  "metadata": {
    "created": "{{timestamp}}",
    "author": "{{author}}"
  }
}
